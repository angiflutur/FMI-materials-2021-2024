import { MouseEventsDirective } from './mouse-events.directive';

describe('MouseEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseEventsDirective();
    expect(directive).toBeTruthy();
  });
});
